[pytest]
# `src` en el pythonpath para que `import config` y `from phase2... import` resuelvan
# igual que en runtime (donde cada módulo hace sys.path.insert(0, src)).
pythonpath = src
testpaths = tests
addopts = -ra -q
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
