% Appendix C — Diagramas teóricos (imágenes + descripción)
% Imágenes exportadas desde DeepWiki (Codemap/Deep Research) a Figures/deepwiki/.
% Reutiliza \dwfig definido en AppendixA (\providecommand por si se compila suelto).

\chapter{Diagramas teóricos}
\label{AppendC}

Apéndice visual de los fundamentos del método: el proceso de difusión que genera
la imagen y las variantes del regresor que mapea la actividad fMRI al espacio
CLIP. Los esquemas se derivan del código y la teoría vía DeepWiki.

% ---------------------------------------------------------------------------
\providecommand{\dwfig}[3]{%
  \begin{figure}[htbp]
    \centering
    \IfFileExists{#1}{\includegraphics[width=\linewidth]{#1}}{%
      \fbox{\parbox[c][0.28\textheight][c]{0.9\linewidth}{\centering
        \emph{[Figura pendiente: exportar desde DeepWiki a}\\\texttt{\detokenize{#1}}\emph{]}}}}
    \caption{#2}
    \label{#3}
  \end{figure}
  \clearpage
}
% ---------------------------------------------------------------------------

\dwfig{Figures/deepwiki/proceso_difusion.png}
  {Proceso de difusión de SD~2.1 unCLIP. El forward agrega ruido gaussiano en
   $T$ pasos hasta $x_T\sim\mathcal{N}(0,I)$; el reverse, guiado por el embedding
   CLIP predicho desde fMRI y por \emph{classifier-free guidance}
   (\texttt{guidance\_scale}$=8.0$ en \texttt{src/config.py::SD\_CONFIG}),
   reconstruye la imagen. Implementado en \texttt{src/sd\_decoder.py}.}
  {fig:proceso_difusion}

\dwfig{Figures/deepwiki/ridge_vs_mindeye.png}
  {Comparación de los tres adaptadores fMRI$\rightarrow$CLIP: Ridge de solución
   cerrada $(X^\top X+\lambda I)^{-1}X^\top Y$
   (\texttt{phase2/adapter\_ridge.py}), Ridge estocástico por SGD
   (\texttt{phase2/adapter\_ridge\_stoch.py}, peldaño de contribución) y backbone
   MindEye con pérdida InfoNCE (\texttt{phase2/mindeye\_models.py}). Se resalta la
   renormalización del embedding (\texttt{norm\_mode} en
   \texttt{visual\_evaluator.py}) previa a SD~2.1 unCLIP.}
  {fig:ridge_vs_mindeye}
